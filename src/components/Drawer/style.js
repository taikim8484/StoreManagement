import { StyleSheet } from "react-native";

export default (styles = StyleSheet.create({
  textTouch: {
    fontSize: 15,
    fontFamily: "Roboto-Light",
    color: "white",
    marginTop: 30,
    marginLeft: 10
  },
  iconTouch: {
    width: 20,
    height: 20,
    marginTop: 30
  }
}));
